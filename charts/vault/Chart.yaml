---
apiVersion: v2
name: vault
description: HashiCorp Vault chart wrapper with custom values
type: application
version: 0.1.0
appVersion: "1.20.4"

dependencies:
  - name: vault
    version: "0.31.0"
    repository: "https://helm.releases.hashicorp.com"
